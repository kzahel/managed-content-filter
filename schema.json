{
  "type":"object",
  "properties": {
    "OnlyGETDomains": {
      "type": "array",
      "description": "Only allow GET requests on these domains",
      "items": {
        "type":"string"
      }
    },
    "post_whitelist": {
      "type": "array",
      "description": "urls to allow POST requests",
      "items": {
        "type":"string"
      }
    },
    "put_whitelist": {
      "type": "array",
      "description": "urls to allow PUT requests",
      "items": { 
       "type":"string"
      }
    }
  }
}
